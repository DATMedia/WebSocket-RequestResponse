function EnrollmentClient(clientRequestDispatcher, protoBufLoader,log, builders) {
    this.beginEnrollment = function () {
        var initialRequest = builders.InitialRequestBuilder.build("InitialRequest");


    };
}